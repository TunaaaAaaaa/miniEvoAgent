[project]
name = "evo-repro"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=1",
    "pydantic>=2",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
